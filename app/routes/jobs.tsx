import type { LoaderFunctionArgs, MetaFunction } from "react-router";
import { useLoaderData, Link, Form } from "react-router";
import { createDb } from "~/db/index.server";
import { getRecruitmentsList } from "~/models/recruitment.server";
import { getRecruitmentApplicationStatus } from "~/lib/recruitment-status";

const INDIAN_STATES = [
  "Andhra Pradesh", "Arunachal Pradesh", "Assam", "Bihar", "Chhattisgarh",
  "Goa", "Gujarat", "Haryana", "Himachal Pradesh", "Jharkhand", "Karnataka",
  "Kerala", "Madhya Pradesh", "Maharashtra", "Manipur", "Meghalaya", "Mizoram",
  "Nagaland", "Odisha", "Punjab", "Rajasthan", "Sikkim", "Tamil Nadu",
  "Telangana", "Tripura", "Uttar Pradesh", "Uttarakhand", "West Bengal",
];

export const meta: MetaFunction = () => [{ title: "Government Jobs 2026 | MyJob Recruitment" }, { name: "description", content: "Search open, upcoming and closed government recruitment notifications by keyword, state and qualification." }];

export const loader = async ({ request }: LoaderFunctionArgs) => { const url = new URL(request.url); const state = url.searchParams.get("state") || undefined; const qualification = url.searchParams.get("qualification") || undefined; const search = url.searchParams.get("search") || undefined; const status = (url.searchParams.get("status") as "open" | "closed" | "upcoming" | null) || undefined; const db = await createDb(); const jobs = await getRecruitmentsList(db, { state, qualification, search, status }); return { jobs, filters: { state, qualification, search, status } }; };

export default function JobsListing() { const { jobs, filters } = useLoaderData<typeof loader>(); return <div className="min-h-screen bg-gray-50 text-gray-900 font-sans"><header className="bg-white border-b px-4 py-4 md:px-8"><div className="max-w-7xl mx-auto flex flex-wrap gap-4 justify-between items-center"><Link to="/" className="text-xl font-bold text-blue-700">MyJob Recruitment</Link><nav className="flex gap-4 text-sm"><Link to="/articles" className="font-semibold text-blue-600">Career Guides</Link><Link to="/teacher-recruitment" className="font-semibold text-blue-600">Teacher Recruitment</Link><Link to="/">Home</Link></nav></div></header><main className="max-w-7xl mx-auto w-full px-4 py-8 md:px-8 flex flex-col md:flex-row gap-8"><aside className="w-full md:w-64 flex-shrink-0"><div className="bg-white border rounded-lg p-5 shadow-sm md:sticky md:top-4"><h2 className="font-bold text-lg mb-4">Find Jobs</h2><Form method="get" className="space-y-4"><input name="search" defaultValue={filters.search} placeholder="Teacher, Police, Clerk..." className="w-full border rounded px-3 py-2 text-sm" /><select name="status" defaultValue={filters.status || ""} className="w-full border rounded px-3 py-2 text-sm bg-white"><option value="">All Statuses</option><option value="open">Open</option><option value="upcoming">Upcoming</option><option value="closed">Closed</option></select><select name="state" defaultValue={filters.state || ""} className="w-full border rounded px-3 py-2 text-sm bg-white"><option value="">All India</option>{INDIAN_STATES.map((stateName) => <option key={stateName} value={stateName}>{stateName}</option>)}</select><select name="qualification" defaultValue={filters.qualification || ""} className="w-full border rounded px-3 py-2 text-sm bg-white"><option value="">Any Qualification</option><option value="10th">10th Pass</option><option value="12th">12th Pass</option><option value="Graduate">Graduate</option><option value="B.Ed">B.Ed / Teacher</option><option value="ITI">ITI / Diploma</option></select><button type="submit" className="w-full bg-blue-600 text-white font-semibold py-2 rounded">Search Jobs</button>{(filters.state || filters.qualification || filters.search || filters.status) && <Link to="/jobs" className="block text-center text-sm text-red-600">Clear filters</Link>}</Form></div></aside><section className="flex-grow"><div className="mb-5"><h1 className="text-2xl md:text-3xl font-bold">{filters.status ? `${filters.status[0].toUpperCase()}${filters.status.slice(1)} Government Jobs` : filters.search ? `Government Jobs for “${filters.search}”` : "Latest Government Jobs"}</h1><p className="text-gray-600 mt-2">{jobs.length} matching recruitment{jobs.length === 1 ? "" : "s"}. Check the official notification before applying.</p></div>{jobs.length === 0 ? <div className="bg-white border rounded-lg p-12 text-center text-gray-500">No recruitments found. Try another filter.</div> : <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">{jobs.map((job) => { const status = getRecruitmentApplicationStatus(job.applicationStartDate, job.applicationLastDate); return <article key={job.id} className="bg-white border rounded-xl p-5 shadow-sm"><div className="flex justify-between gap-3 mb-2"><span className="text-xs font-semibold bg-gray-100 px-2 py-1 rounded">{status.toUpperCase()}</span>{job.state && <span className="text-xs bg-gray-100 px-2 py-1 rounded">{job.state}</span>}</div><p className="text-xs font-bold text-gray-500 uppercase tracking-wide">{job.organisation}</p><h2 className="text-lg font-semibold mt-1 mb-4">{job.title}</h2><div className="grid grid-cols-2 gap-3 text-sm mb-5"><div><span className="block text-gray-500 text-xs">Vacancies</span><b>{job.numberOfPosts ?? "See notice"}</b></div><div><span className="block text-gray-500 text-xs">Qualification</span><b>{job.minimumQualification || "See notice"}</b></div><div><span className="block text-gray-500 text-xs">Application Starts</span><b>{job.applicationStartDate ? new Date(job.applicationStartDate).toLocaleDateString("en-IN") : "TBA"}</b></div><div><span className="block text-gray-500 text-xs">Last Date</span><b>{job.applicationLastDate ? new Date(job.applicationLastDate).toLocaleDateString("en-IN") : "TBA"}</b></div></div><Link to={`/recruitment/${job.slug}`} className="block text-center bg-blue-600 text-white text-sm font-semibold py-2.5 rounded-lg">View Recruitment Details</Link></article>; })}</div>}</section></main></div>; }
